{
  "name": "mamometro-cs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Mamômetro CS — ranking oficial das mamadas (quem não bateu a meta mama quem bateu)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "serve": "npm run build && node server.js",
    "demo": "node tools/mamometro-demo.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@laihoe/demoparser2": "^0.41.3",
    "firebase": "^10.12.2",
    "firebase-admin": "^13.0.0",
    "vite": "^5.4.0"
  },
  "devDependencies": {
    "prettier": "^3.3.0"
  }
}
